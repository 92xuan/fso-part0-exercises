```mermaid
sequenceDiagram;
    participant browser
    participant server

    browser->>server: POST https://fullstack-exampleapp.herokuapp.com/new_note_spa
    Note right of browser: Page reload prevented, form data sent as JSON in background
    activate server
    server-->>browser: HTTP Status Code 201 - Request Succeeded
    deactivate server
    Note right of browser: DOM Updated to reflect new submission

```